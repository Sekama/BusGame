using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ChargingAnimation : MonoBehaviour
{
    /*[SerializeField] private Animator particleCharge01, particleCharge02, particleCharge03, particleCharge04;  
    [SerializeField] private Animator particleArea;  

    private void OnTriggerStay(Collider other)
    {
        if (other.gameObject.tag == "Player") 
        {
            particleCharge01.SetBool("isCharging", true);
            particleCharge02.SetBool("isCharging", true);
            particleCharge03.SetBool("isCharging", true);
            particleCharge04.SetBool("isCharging", true);
            particleArea.SetBool("isCharging", true);
        }    
    }

    private void OnTriggerExit(Collider other)
    {
        if (other.gameObject.tag == "Player") 
        {
            particleCharge01.SetBool("isCharging", false);
            particleCharge02.SetBool("isCharging", false);
            particleCharge03.SetBool("isCharging", false);
            particleCharge04.SetBool("isCharging", false);
            particleArea.SetBool("isCharging", false);
        }        
    }*/
}
